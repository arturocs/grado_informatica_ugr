a) verdadero
b) falso, ya que el operador > en c++ es usado para expersiones booleanas
c) falso, daría como resultado el valor de la variable a
d) falso, ya que el operador > en c++ es usado para expersiones booleanas
e) Da error ya que el valor de una constante no se puede cambiar,
   y el valor de la constante debe ser definido cuando la constante es declarada
